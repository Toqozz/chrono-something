(
  axes: {
    "vertical": Emulated(pos: Key(Comma), neg: Key(O)),
    "horizontal": Emulated(pos: Key(E), neg: Key(A)),
  },
  actions: {
    // cheats.
    "timescale_faster": [[Key(S)]],
    "timescale_slower": [[Key(L)]],
  },
)